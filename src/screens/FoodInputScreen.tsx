import { Text, View } from "react-native";

export default function FoodInputScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>음식 입력 화면 (준비 중)</Text>
    </View>
  );
}
